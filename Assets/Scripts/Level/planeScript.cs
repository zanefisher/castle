using UnityEngine;
using System.Collections;

public class planeScript : MonoBehaviour {
	
	/*void OnMouseOver(){
		if (Input.GetMouseButtonDown (0)) {
			StructureController.selectedStructure = null;
			Debug.Log (StructureController.selectedStructure.name);
		}
	}*/
}
